# STT Sidecar dependencies
# Install via: stt/setup.bat (Windows) or stt/setup_vysper_stt.sh (Unix/Mac)

# Speech-to-text (faster reimplementation of Whisper via CTranslate2)
faster-whisper>=1.1.0

# Audio capture (cross-platform, pre-built binaries)
sounddevice>=0.4.6

# Numerical computing (audio buffer operations)
numpy>=1.24.0

# VAD + tensor/audio ops (silero-vad loads via torch.hub)
# CPU-only torch/torchaudio are installed separately in setup script.
torch>=2.1.0
torchaudio>=2.1.0

# Optional speaker diarization for long Secretaria meeting mode.
# Requires a Hugging Face token and accepting the model terms before first use.
pyannote.audio>=3.3.0

# Required by pyannote/embedding (voiceprint enrollment/matching, see
# stt/voiceprint_store.py) to load its legacy hydra/omegaconf-based config.
omegaconf>=2.3.0

# Local text-to-speech for secretaria mode (Ctrl+3)
piper-tts>=1.2.0
edge-tts>=7.0.0
